name: Haskell CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - uses: actions/cache@v1
      with:
        path: ~/.stack
        key: ${{ runner.os }}-packageyaml-${{ hashFiles('package.yaml') }}
        restore-keys: |
          ${{ runner.os }}-packageyaml-
    - name: Build
      run: |
        stack build
    - name: Test
      run: |
        stack test
